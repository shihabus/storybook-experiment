const unit = 2;

// Todo add px / em
const spacing = {
  xxsmall: `${unit * 1}px`, // 2
  xsmall: `${unit * 2}px`, // 4
  small: `${unit * 3}px`, // 6
  default: `${unit * 4}px`, // 8
  medium: `${unit * 6}px`, // 12
  large: `${unit * 8}px`, // 16
  xlarge: `${unit * 12}px`, // 24
  xxlarge: `${unit * 16}px` // 32
};

export default spacing;

import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { withKnobs, text, color } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { UserInfoCard } from "../componentlib";

<Meta title="COMPONENTS|Cards" />

# User Info

<Preview withSource='none'>
<Story name="User Info" parameters={{ decorators: [withKnobs] }}>
  <div style={{height:'200px'}}>
    <UserInfoCard onLogOutClicked={action('onLogOutClicked')} />
    <h3> A simple User Info card that we are using for showing the user Info </h3>
    <h4> This comes with an dropdown icon where we have logout Option </h4>
  </div>
</Story>
</Preview>


## API

<Props of={UserInfoCard}/>
